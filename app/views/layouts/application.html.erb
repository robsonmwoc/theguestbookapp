<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>The Guestbook App</title>

    <!-- Bootstrap -->
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= csrf_meta_tags %>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <div class="jumbotron">
        <h1>The Guestbook App</h1>
      </div>
    </div>

    <div class="container">
      <%= content_tag(:div, flash[:notice], class: 'alert alert-success', role: 'alert') if flash[:notice] %>
      <%= content_tag(:div, flash[:alert], class: 'alert alert-success', role: 'alert') if flash[:alert] %>
    </div>

    <%= render 'entries/add_form' %>

    <%= yield %>

    <%= javascript_include_tag 'application' %>
  </body>
</html>
